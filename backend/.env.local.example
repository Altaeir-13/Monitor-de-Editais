# Database local SQLite
# POSTGRES_* are kept only because the current backend settings require them.
POSTGRES_USER=local
POSTGRES_PASSWORD=local
POSTGRES_DB=monitor_editais_local
DATABASE_URL=sqlite:///./app.db

# Security
SECRET_KEY=dev-local-change-me
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS
BACKEND_CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]

# SMTP local/mock
SMTP_TLS=true
SMTP_PORT=587
SMTP_HOST=
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=no-reply@example.com

# Crawler scheduler
CRAWLER_SCHEDULER_ENABLED=false
CRAWLER_INTERVAL_MINUTES=360

# Local admin/smoke helpers. Do not commit real passwords in backend/.env.
ADMIN_NAME=Administrador
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=
SMOKE_BASE_URL=http://localhost:8000
SMOKE_ADMIN_EMAIL=admin@example.com
SMOKE_ADMIN_PASSWORD=
SMOKE_TIMEOUT=10
